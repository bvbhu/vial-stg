# 二进制文件必须绕开 core.autocrlf 的换行转换，否则字体/图片检出后即损坏
#
# 字体走 Git LFS：Noto Sans SC 单文件 7.95 MB，直接入库会让每次克隆永久背上
# 这份体积，且后续任何字体更新都无法回收。*.woff/图片等其余二进制仍走普通
# binary 属性（它们目前都很小）。
#
# 注意：LFS 需要 `git lfs install` 且首次迁移历史要用 `git lfs migrate import`
# （见 README「字体与 Git LFS」一节），仅加下面的属性不影响已提交的历史对象。
*.otf filter=lfs diff=lfs merge=lfs -text
*.ttf filter=lfs diff=lfs merge=lfs -text
*.ttc filter=lfs diff=lfs merge=lfs -text
*.woff binary
*.woff2 binary
*.png binary
*.ico binary
*.icns binary
*.svg text eol=lf
